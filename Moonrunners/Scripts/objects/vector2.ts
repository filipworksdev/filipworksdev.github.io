module objects {
    export class Vector2 {

        // Constructors
        constructor(x: number, y: number)
        {
            this.x = x;
            this.y = y;
        }

        // Public properties
        public x : number;
        public y : number;

        // Public Methods

        
    }
}